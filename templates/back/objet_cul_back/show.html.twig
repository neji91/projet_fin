{% extends 'base.html.twig' %}

{% block title %}Objet culturelle{% endblock %}

{% block body %}
    <h1>Objet culturelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objetCul.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ objetCul.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ objetCul.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ objetCul.region }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ objetCul.categorie }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ objetCul.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('objet_cul_back') }}">back to list</a>

    <a href="{{ path('objetCul_edit', {'id': objetCul.id}) }}">edit</a>

    {{ include('back/objet_cul_back/_delete_form.html.twig') }}
{% endblock %}
