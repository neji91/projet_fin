<form method="post" action="{{ path('objetCul_delete', {'id': objetCul.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objetCul.id) }}">
    <button class="btn">Delete</button>
</form>
